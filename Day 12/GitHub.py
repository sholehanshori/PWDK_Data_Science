#  ------------------------ Kamis, 7 November 2019 -----------------------------------

# staging area => add
# commit => commit
# git add .
# git status
# git rm --cached cobalagi.py
# git commit -m "checkpoint level 1"
# git log (cek komit)
# git log --oneline (cek komit yg penting)
# git checkout master
# git checkout 43678ca (dapat nomor identitas dari 'git log --oneline')
# git reset 43678ca (untuk menghapus)

# git branch cihuy (membuat branch dengan nama yang di inginkan)
# git branch -a (cek branch yang dimiliki)
# git checkout -b cihuy  (switch ke cihuy)
# git checkout cihuy  (switch master ke brand)
# git checkout master (ngecek lokasi apa sudah di master)
# git branch -D cihuy (menghapus branch)
# git merge cihuy     (merge with master)

# git remote add (namafile) (link github)
# git push (namafile) master
# git clone link
